apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcp.fullname" . }}-test-connection"
  labels:
    {{- include "gcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
