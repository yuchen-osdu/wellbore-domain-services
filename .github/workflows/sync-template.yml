name: Sync Template

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  open-acceptance-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@67018539274d69449ef7c02e8e71183d1719ab42  # v2.1.4
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Open acceptance runner pull request
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh pr create \
            --repo "$GITHUB_REPOSITORY" \
            --base main \
            --head feat/issue-15-python-acceptance \
            --title "test: add SPI live acceptance runner" \
            --body "Adds the descriptor-owned pytest runner for the direct SPI deploy lane and codifies the proven basic, CRUD, and bulk-statistics suite. Part of #15."
